/**
 * @public
 */
export interface Options {}

/**
 *
 * return `poxlsx` string
 *
 * @returns the `poxlsx`  string
 *
 * @example
 * Here's an example:
 *
 * ```ts
 * import { Poxlsx } from 'poxlsx'
 *
 * console.log(Poxlsx());
 * // Prints "poxlsx":
 * ```
 */
export function Poxlsx(options?: Options) {
  return 'poxlsx';
}
